# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Secrets. Never commit an API key.
.env
*.key

# Node
node_modules/
dist/
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# Ad-hoc run recordings. The named case folders beside it are committed.
backend/recordings/_latest/

# Note: the curated cases in backend/recordings/ are deliberately NOT ignored. Those are saved model
# responses so the project can be run with `--replay` and no API key at all.
